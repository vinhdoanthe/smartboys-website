<section class="bg-gray-800 py-32">
  <div class="max-w-4xl mx-auto px-4">
    <h1 class="text-4xl font-bold text-white mb-8">Trang thông tin chính thức đội bóng SmartBoys</h1>
    <p class="text-lg text-gray-400 mb-8">SmartBoys FC là đội bóng tập hợp của các anh em đam mê bóng đá trong khu đô thị Vinhomes Smart City. Các thành viên có thể đá bóng không hay, nhưng nhiệt tình và đá đẹp. Tiêu chí của đội bóng SmartBoys <strong>Chơi bóng phải vui, Vui mới chơi bóng</strong></p>
    <a href="#" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-full">Get Started</a>
  </div>
</section>
<section class="bg-gray-100 py-12">
  <div class="max-w-4xl mx-auto px-4">
    <h2 class="text-3xl font-semibold text-gray-800 mb-6">Upcoming Matches</h2>
    <% if upcoming_matches.blank? %>
      <p class="text-gray-600">No upcoming matches</p>
    <% else %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <%= render(MatchCardComponent.with_collection(upcoming_matches)) %>
      </div>
    <% end %>
  </div>
</section>

<section class="bg-gray-300 py-12">
  <div class="max-w-4xl mx-auto px-4">
    <h2 class="text-3xl font-semibold text-gray-800 mb-6">Past Matches</h2>
    <% if past_matches.blank? %>
      <p class="text-gray-600">No past matches</p>
    <% else %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <%= render(MatchCardComponent.with_collection(past_matches)) %>
      </div>
    <% end %>
  </div>
</section>

<section class="bg-gray-100 py-12">
  <div class="max-w-4xl mx-auto px-4">
    <h2 class="text-2xl font-bold text-gray-800 mb-4">Our Sponsors</h2>
    <div class="flex flex-wrap justify-center">
      <%= render(SponsorComponent.with_collection(sponsors)) %>
    </div>
  </div>
</section>
